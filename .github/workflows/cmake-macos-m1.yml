name: CMake MacOS M1 Build

on: [push, pull_request]

env:
  BUILD_TYPE: Release

jobs:
  macos-m1-build:
    runs-on: macos-11
    
    steps:
      - uses: actions/checkout@v2
        with:
            submodules: 'true'

      - name: Restore artifacts, or run vcpkg, build and cache artifacts
        uses: lukka/run-vcpkg@v7
        id: runvcpkg
        with:
            vcpkgArguments: 'discord-rpc zlib rapidjson openssl'
            vcpkgDirectory: '${{ runner.workspace }}/b/vcpkg'
            vcpkgGitCommitId: '40616a5e954f7be1077ef37db3fbddbd5dcd1ca6'
            vcpkgTriplet: 'arm64-osx'

      - name: Create Build Environment
        run: cmake -E make_directory ${{github.workspace}}/build-macos-m1

      - name: Configure CMake
        shell: bash
        working-directory: ${{github.workspace}}/build-macos-m1
        run: cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=$BUILD_TYPE -DCMAKE_TOOLCHAIN_FILE='${{ runner.workspace }}/b/vcpkg/scripts/buildsystems/vcpkg.cmake' -DVCPKG_TARGET_TRIPLET=arm64-osx

      - name: Build
        working-directory: ${{github.workspace}}/build-macos-m1
        shell: bash
        run: cmake --build . --config $BUILD_TYPE

      - name: Archive artifacts
        uses: actions/upload-artifact@v3
        with:
          name: BeamMP-Launcher
          path: ${{github.workspace}}/build-macos-m1/Release/BeamMP-Launcher
